<h3>
  <div><%= pluralize(post.points, 'point') %></div>
  <div>
    <small>
      <%= pluralize(post.up_votes, 'up vote') %>,
      <%= pluralize(post.down_votes, 'down vote') %>
    </small>
  </div>
</h3>
